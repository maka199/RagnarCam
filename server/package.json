{
  "name": "ragnarcam-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": ">=18 <=22"
  },
  "scripts": {
    "start": "node index.js",
    "build:client": "cd ../client && NPM_CONFIG_PRODUCTION=false npm ci && npm run build && rm -rf ../server/client-dist && cp -r dist ../server/client-dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0",
    "cors": "^2.8.5"
  }
}
